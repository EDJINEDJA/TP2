import numpy as np

def clés(dim_wk : tuple, F : np.array):
    """Define valeurs matrix 
        Args : 
            dim_wk : tuple (dimension of the matrix w_k)
            F : np.array (the matrix of features)
        Return :
            K : np.array (W_k@F^T)
    """
    

    return NotImplementedError
