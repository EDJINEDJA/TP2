import numpy as np

def valeurs(dim_wv : tuple, F : np.array):
    """Define valeurs matrix 
        Args : 
            dim_wv : tuple (dimension of the matrix w_v)
            F : np.array (the matrix of features)
        Return :
            V : np.array (W_v@F^T)
    """

    return NotImplementedError