import numpy as np

def similarité(K : np.array  ,Q : np.array):
     """Matrix Multiplication K^T.Q
        Args : 
            K : np.array (the matrix K)
            Q : np.array (the matrix Q)
        Return :
            dotProduct : np.array (K^T.Q)
    """
    return NotImplementedError